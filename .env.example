# Server only. No secret may be placed in browser JavaScript or committed here.
# Inactive until domain/DNS, access restrictions, retention and real delivery are validated.
SUPPORT_RECEIVE_ENABLED=false
SUPPORT_SEND_ENABLED=false
SUPPORT_AI_ENABLED=false
SUPPORT_INBOX_ADDRESSES=
SUPPORT_FROM_EMAIL=
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
# Explicitly choose an available model that supports Responses structured outputs.
SUPPORT_AI_MODEL=
