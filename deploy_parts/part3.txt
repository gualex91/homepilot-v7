;let p=await sb.from('properties').select('*').eq('household_id',h.id).order('created_at');props=p.data||[];ap=props[0]||null;await lp();render()}async function createHousehold(){let r=await sb.from('households').insert({name:hn.value.trim()||'Mon foyer',created_by:u.id}).select().single();if(r.error)return alert(r.error.message);await load()}function choices(){ec.innerHTML=E.map(([k,l])=>`<label class="choice"><input type="checkbox" name="e" value="${k}" onchange="smart()">${l}</label>`).join('');sc.innerHTML=S.map(([k,l])=>`<label class="choice"><input type="checkbox" name="s" value="${k}">${l}</label>`).join('')}function smart(){let a=[...document.querySelectorAll('[name=e]:checked')].map(x=>x.value);pool.classList.toggle('hidden',!a.includes('piscine'));spa.classList.toggle('hidden',!a.includes('spa'))}async function createProperty(){let name=pname.value.trim();if(!name)return alert('Donne un nom à la propriété.');let a=[...document.querySelectorAll('[name=e]:checked')].map(x=>x.value),sv=[...document.querySelectorAll('[name=s]:checked')].map(x=>x.value);cp.disabled=true;let r=await sb.from('properties').insert({household_id:h.id,name,property_type:ptype.value,city:pcity.value.trim(),postal_code:ppostal.value.trim(),construction_year:pyear.value?+pyear.value:null,created_by:u.id}).select().single();if(r.error){cp.disabled=false;return alert(r.error.message)}for(const k of a){let meta=E.find(x=>x[0]===k),details={};if(k==='piscine')details={size:poolSize.value,treatment:poolTreat.value,volume_estimated_l:PV[poolSize.value]||null,volume_actual_l:+poolVol.value||null,volume_l:+poolVol.value||PV[poolSize.value]||null};if(k==='spa')details={size:spaSize.value,treatment:spaTreat.value,volume_estimated_l:SV[spaSize.value]||null,volume_actual_l:+spaVol.value||null,volume_l:+spaVol.value||SV[spaSize.value]||null};let x=await sb.from('equipment').insert({property_id:r.data.id,equipment_type:k,name:meta[1].replace(/^[^ ]+ /,''),details,created_by:u.id});if(x.error)alert(x.error.message)}if(sv.length){let x=await sb.from('property_services').insert(sv.map(k=>({property_id:r.data.id,service_type:k,name:S.find(z=>z[0]===k)[1].replace(/^[^ ]+ /,''),created_by:u.id})));if(x.error)alert(x.error.message)}pf.classList.add('hidden');cp.disabled=false;await load();ap=props.find(x=>x.id===r.data.id)||r.data;await lp();render();show('home')}async function lp(){if(!ap){tasks=[];eq=[];return}let[a,b]=await Promise.all([sb.from('tasks').select('*').eq('property_id',ap.id).order('due_at'),sb.from('equipment').select('*').eq('property_id',ap.id).order('created_at')]);tasks=a.data||[];eq=b.data||[]}async function activate(id){ap=props.find(x=>x.id===id);await lp();render();show('home')}async function createTask(){if(!ap)return alert('Ajoute ou ouvre une propriété.');let title=tt.value.trim();if(!title)return alert('Entre un nom pour la tâche.');let r=await sb.from('tasks').insert({property_id:ap.id,title,category:'Entretien',due_at:td.value||null,status:'todo',created_by:u.id});if(r.error)return alert(r.error.message);tt.value='';tf.classList.add('hidden');await lp();render()}async function toggle(id,st){let n=st==='done'?'todo':'done',r=await sb.from('tasks').update({status:n,completed_by:n==='done'?u.id:null,completed_at:n==='done'?new Date().toISOString():null}).eq('id',id);if(r.error)return alert(r.error.message);await lp();render()}async function invite(){let r=await sb.from('household_invitations').insert({household_id:h.id,property_id:ip.value,email:ie.value.trim().toLowerCase(),role:ir.value,invited_by:u.id});if(r.error)return alert(r.error.message);alert('Invitation enregistrée.')}function fd(v){if(!v)return '';let d=new Date(v+'T12:00:00');return d.toLocaleDateString('fr-CA',{day:'numeric',month:'long',year:'numeric'})}function tc(t){return `<div class="card"><div class="row"><div><b>${t.title}</b><div class="muted">${fd(t.due_at)}</div></div><span class="pill">${t.status==='done'?'Fait':'À faire'}</span></div><div class="taskactions"><button class="alt" onclick="toggle('${t.id}','${t.status}')">${t.status==='done'?'Réouvrir':'C’est fait'}</button>${t.due_at?`<button class="alt" onclick="exportICS('${t.id}')">📅 Apple / Android</button>`:''}</div></div>`}function render(){pn.textContent=ap?.name||'Aucune propriété';due.textContent=tasks.filter(x=>x.status==='todo').length;soon.textContent=tasks.filter(x=>x.status==='soon').length;done.textContent=tasks.filter(x=>x.status==='done').length;tl.innerHTML=tasks.slice(0,4).map(tc).join('')||'<div class="card muted">Aucune tâche.</div>';at.innerHTML=tasks.map(tc).join('')||'<div class="card muted">Aucune tâche.</div>';pl.innerHTML=props.map(x=>`<div class="card row"><div><b>${x.name}</b><div class="muted">${x.city||''}</div></div><button class="alt" onclick="activate('${x.id}')">Ouvrir</button></div>`).join('')||'<div class="card muted">Ajoute ta première propriété.</div>';el.innerHTML=eq.map(x=>{let d=x.details||{},v=d.volume_l||d.volume_actual_l||d.volume_estimated_l||0;return `<div class="card"><b>${x.name}</b>${v?`<p class="muted">Volume : ${Number(v).toLocaleString('fr-CA')} L</p><div class="notice"><b>Estimation HomePilot</b> — Toujours vérifier l’étiquette du produit et le manuel du fabricant avant l’utilisation. En cas de divergence, les instructions du fabricant ont priorité.</div>`:''}</div>`}).join('')||'<div class="card muted">Aucun équipement.</div>';ip.innerHTML=props.map(x=>`<option value="${x.id}">${x.name}</option>`).join('');renderCalendar()}function o