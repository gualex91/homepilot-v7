penTaskForm(date=''){tf.classList.remove('hidden');if(date)td.value=date;setTimeout(()=>tt.focus(),50)}function ymd(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function renderCalendar(){if(!document.getElementById('cal'))return;let y=calCursor.getFullYear(),m=calCursor.getMonth();cmLabel.textContent=new Date(y,m,1).toLocaleDateString('fr-CA',{month:'long',year:'numeric'});let days=['L','M','M','J','V','S','D'];let html=days.map(x=>`<div class="caldow">${x}</div>`).join('');let first=new Date(y,m,1),offset=(first.getDay()+6)%7,last=new Date(y,m+1,0).getDate(),today=ymd(new Date());for(let i=0;i<offset;i++)html+='<div class="calday empty"></div>';for(let d=1;d<=last;d++){let ds=ymd(new Date(y,m,d)),n=tasks.filter(t=>t.due_at===ds).length,cls='calday'+(ds===today?' today':'')+(n?' has':'');html+=`<div class="${cls}"><button onclick="openTaskForm('${ds}')">${d}</button>${n?`<div class="muted"><span class="dot"></span>${n} tâche${n>1?'s':''}</div>`:''}</div>`}cal.innerHTML=html}function changeMonth(n){calCursor=new Date(calCursor.getFullYear(),calCursor.getMonth()+n,1);renderCalendar()}function icsText(list){let lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Nuvabri//Calendrier//FR','CALSCALE:GREGORIAN','METHOD:PUBLISH'];for(const t of list){if(!t.due_at)continue;let ds=t.due_at.replaceAll('-','');let end=new Date(t.due_at+'T12:00:00');end.setDate(end.getDate()+1);let de=ymd(end).replaceAll('-','');let title=String(t.title||'Tâche Nuvabri').replace(/[\r\n]+/g,' ');lines.push('BEGIN:VEVENT','UID:'+t.id+'@homepilot','DTSTAMP:'+new Date().toISOString().replace(/[-:]/g,'').replace(/\.\d{3}Z/,'Z'),'DTSTART;VALUE=DATE:'+ds,'DTEND;VALUE=DATE:'+de,'SUMMARY:'+title,'DESCRIPTION:Tâche Nuvabri','END:VEVENT')}lines.push('END:VCALENDAR');return lines.join('\r\n')}function dlICS(text,name){let blob=new Blob([text],{type:'text/calendar;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}function exportICS(id){let t=tasks.find(x=>x.id===id);if(t)dlICS(icsText([t]),'nuvabri-'+t.due_at+'.ics')}function exportAllICS(){let list=tasks.filter(t=>t.due_at&&t.status!=='done');if(!list.length)return alert('Aucune tâche datée à exporter.');dlICS(icsText(list),'nuvabri-calendrier.ics')}function show(id){document.querySelectorAll('.screen').forEach(x=>x.classList.remove('on'));document.getElementById(id).classList.add('on')}const ideas={spring:['🌱','Printemps','Nettoyage de printemps','Fais un tour rapide des filtres, détecteurs et gouttières.','Commence par 15 minutes : filtres, piles de détecteurs et drains extérieurs.'],summer:['🍋','Été','Limonade maison','Une idée simple pour profiter du patio.','Mélange 1 L d’eau, le jus de 4 citrons et sucre au goût.'],autumn:['☕','Automne','Chocolat chaud maison','Une recette réconfortante pour une soirée fraîche.','Pour 2 tasses : 500 mL de lait, 2 c. à soupe de cacao, sucre au goût et un peu de vanille.'],winter:['❄️','Hiver','Ambiance cocooning','Une petite soirée maison avec boisson chaude et lumière douce.','Prépare une boisson chaude et profite d’un moment tranquille.'],christmas:['🎄','Décembre • Noël','Chocolat chaud des Fêtes','Une version festive du chocolat chaud pour décembre.','Ajoute cannelle, mini-guimauves ou copeaux de chocolat.']};let ci;function season(){let d=new Date(),m=d.getMonth()+1,k=m===12?'christmas':m>=3&&m<=5?'spring':m>=6&&m<=8?'summer':m>=9&&m<=11?'autumn':'winter',x=ideas[k];ci=x;se.textContent=x[0];sl.textContent=x[1];it.textContent=x[2];ib.textContent=x[3];let c={spring:['#eef7e8','#fffaf0'],summer:['#fff8df','#eef8f5'],autumn:['#fff1e6','#f4e7d2'],winter:['#eef4f7','#f8fbfc'],christmas:['#edf4ef','#fff1e6']}[k];document.documentElement.style.setProperty('--s1',c[0]);document.documentElement.style.setProperty('--s2',c[1]);if(m===12&&d.getDate()===25)ib.textContent='🎄 Joyeux Noël de la part de Nuvabri. '+x[3];if(m===12&&d.getDate()===31)ib.textContent='✨ Nuvabri te souhaite une excellente soirée du jour de l’An. '+x[3]}function idea(){id.textContent=ci?.[4]||'';id.classList.toggle('hidden')}sb.auth.getSession().then(x=>x.data.session?boot(x.data.session):null);sb.auth.onAuthStateChange((e,s)=>{if(e==='SIGNED_IN'&&s)boot(s)});ra();choices();season();</script></body></html>